const tooltip = document.querySelector('.cards__tooltip');
const button = document.querySelector('.cards__button');
const input = document.querySelector('.cards__input');
const cards = document.querySelectorAll('.card');

export {
  tooltip, button, input, cards,
};
